# ========== AI 总结模型 (可扩展: claude / minimax / openai 等) ==========
# 当前使用: minimax
SUMMARIZER_PROVIDER=minimax
SUMMARY_LANGUAGE=zh-CN

# MiniMax (Anthropic 兼容接口)
MINIMAX_API_KEY=sk-ant-xxx
# MINIMAX_BASE_URL=https://api.minimaxi.com/anthropic
# MINIMAX_MODEL=MiniMax-M2.1

# Claude (Anthropic) - 使用 claude 时配置
# ANTHROPIC_API_KEY=sk-ant-xxx

# OpenAI - 使用 openai 时配置
# OPENAI_API_KEY=sk-xxx

# ========== 通知渠道 (可扩展: email / dingtalk 等) ==========
NOTIFY_CHANNELS=email

# ---------- 163 邮箱 (发件 + 收件) ----------
EMAIL_USER=your@gmail.com
EMAIL_PASS=your_app_password
RECIPIENT_EMAIL=recipient@gmail.com
# 163 SMTP 必须使用以下配置
EMAIL_HOST=smtp.163.com
EMAIL_PORT=465
EMAIL_SECURE=true

# ---------- 钉钉 (可选) ----------
# DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=xxx
# DINGTALK_SECRET=xxx

# ========== 抓取与过滤 ==========
LANGUAGE_FILTER=
SINCE=daily
MAX_PROJECTS=10
# 抓取 GitHub 超时(毫秒)，默认 60000；国内网络慢可设为 90000 或更大
SCRAPER_TIMEOUT_MS=60000
